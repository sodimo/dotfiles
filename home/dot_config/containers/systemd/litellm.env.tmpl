LITELLM_MASTER_KEY=CHANGEME-LITELLM-MASTER-KEY
LITELLM_SALT_KEY=CHANGEME-LITELLM-SALT-KEY
DATABASE_URL=postgresql://litellm:litellm@litellm-postgres:5432/litellm
REDIS_URL=redis://litellm-redis:6379/0

# Cloud escalation keys (replace at chezmoi apply time)
ANTHROPIC_API_KEY=CHANGEME-ANTHROPIC
OPENAI_API_KEY=CHANGEME-OPENAI

# Token-accounting (Principle 2) — direct writes to D1 via the Worker's
# ledger_write tool or via a pipeline hook. See 15-design-principles.md.
STORE_MODEL_IN_DB=true
