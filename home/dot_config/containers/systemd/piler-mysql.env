# piler-mysql — MariaDB server env.
# Passwords mirror piler.env (piler connects with the same creds).
# Replaced at chezmoi apply time by the age-encrypted real-secrets pass.
MARIADB_ROOT_PASSWORD=CHANGEME-PILER-ROOT-PASSWORD
MARIADB_DATABASE=piler
MARIADB_USER=piler
MARIADB_PASSWORD=CHANGEME-PILER-DB-PASSWORD
